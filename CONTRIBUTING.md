# 贡献指南

感谢你愿意让这个工具变得更好。有两条路，**补字段规则**是最有价值的一条。

---

## 一、补字段规则（最需要）

招聘系统千奇百怪，同一件事有十几种叫法：「手机号 / 手机号码 / 联系电话 / Mobile」。

如果你遇到**识别不了的字段**，请把它补进 `assets/字段规则_通用.json`：

```json
{
  "键": "手机号",
  "标签": ["手机号", "手机号码", "联系电话", "电话", "移动电话", "Mobile", "Phone"],
  "必填": true,
  "类型": "text",
  "分组": "联系方式"
}
```

### 规则说明

| 字段 | 说明 |
|---|---|
| `键` | 档案 JSON 里的键名。**同一键只能出现一次** |
| `标签` | 页面上可能出现的所有写法（中英文都可）。匹配时按此索引 |
| `必填` | 是否必填。影响体检报告的「必填项缺失」判定 |
| `类型` | `text` / `select` / `radio` / `date` / `file` / `textarea` / `number` / `checkbox` |
| `分组` | 基本信息 / 联系方式 / 教育背景 / 能力证书 / 求职意向 / 经历与自述 / 附件 |

### 两条约定

1. **附件类字段的键名必须以 `附件_` 开头**（如 `附件_简历`）。
   程序据此把它和普通字段分开处理 —— 附件要上传文件，不走文本填写。
2. **键名以 `_` 开头的条目会被程序跳过**，用于写注释/元数据。

### 提 PR 前请自测

```bash
python scripts/wangshen.py selftest
python -m unittest discover -s tests
```

---

## 二、修 Bug / 加功能

1. 先开 Issue 描述问题，避免无效劳动
2. Fork → 开分支 → 改 → 提 PR
3. PR 里请说明：**改了什么 / 为什么 / 怎么验证的**

### 不可突破的设计约束

提 PR 时请确认没有违反以下三条，违反的 PR 会被拒绝：

| 约束 | 含义 |
|---|---|
| **不代提交** | 任何代码路径都不得点击提交/投递类按钮 |
| **不碰凭据** | 不得读取、填写、存储密码，不得代过人机验证 |
| **值不出本机** | 真实值不得打印到 stdout、不得写入日志、不得发起网络请求 |

---

## 三、代码风格

- Python：**能用标准库就用标准库**。`core.py` / `check_profile.py` /
  `make_sheet.py` / `mask_pii.py` 保持零第三方依赖
- `fill_form.py` 允许依赖 `playwright`，但**必须**在缺依赖时优雅降级
- 中文注释，面向非专业用户解释「为什么」而不只是「做什么」
- 新增脚本请自带 `--selftest`

---

## 四、报告字段识别失败

提 Issue 时请附（**注意脱敏，不要贴真实个人信息**）：

```
页面字段标签：手机号码*
页面所在的系统：北森 / Moka / 大易 / 其他
期望映射到：手机号
实际结果：未命中
```

有了这三行，补规则就是几分钟的事。
