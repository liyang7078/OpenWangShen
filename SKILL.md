---
name: openwangshen
description: 网申表单自动填写技能（本地优先、不代提交）。当用户说「自动填网申」「帮我填这个网申表」「网申自动填写」「把简历信息填进这个页面」「填写投递表单」「autofill this application form」时使用。它打开岗位页、识别表单字段、从本机档案取值填入，然后停下等用户确认；打不开页面时改用「填写清单」模式。**三条红线：绝不代点提交、绝不填密码或验证码、身份信息不出本机。**
---

# OpenWangShen · 网申表单自动填写

> 一个**跑在用户自己电脑上**的表单填写执行器。
> 它只做一件事：**把已经准备好的个人信息，准确填进网申页面。**
> 它不选岗、不改简历、不提交、不联网。

---

## 一、什么时候用它

**用**：
- 用户已经把个人信息整理成 `profile.json`，现在要往某个网申页面里填
- 用户有一堆岗位要投，不想每次重敲 30 个字段

**不用**：
- ❌ 用户还没准备好档案 → 先用 `check_profile.py` 帮他补全
- ❌ 用户要的是「选哪些岗投」 → 那是选岗决策，不是填表
- ❌ 用户要的是「帮我投了别问了」 → **拒绝**。见第四节
- ❌ 公务员 / 事业单位 / 军队文职的**报名系统** → 那是资格报名表，口径不同，本技能不覆盖

---

## 二、标准流程（五步）

| 步 | 动作 | 命令 |
|---|---|---|
| 1 | **档案体检** —— 确认必填项齐全 | `python scripts/wangshen.py check --profile profile.json` |
| 2 | **预演** —— 只映射不写入，看能识别多少字段 | `... fill --profile p.json --url U --dry-run` |
| 3 | **正式填** —— 写入表单并截图 | `... fill --profile p.json --url U --screenshot s.png` |
| 4 | **交人工** —— 报告结果，让用户核对 | （报告 `填表报告.json`） |
| 5 | **用户提交** —— 用户自己点提交 | （本技能不参与） |

**第 2 步不要跳过。** 预演能提前暴露「这个页面字段识别不了」，
避免填一半发现填错。

### 打不开页面时（降级）

```
fill 失败  →  改用 sheet 生成填写清单  →  用户照着粘
```

```bash
python scripts/wangshen.py sheet --profile profile.json \
    --labels-file 页面字段.txt --out 填写清单.md
```

**降级不等于放弃把关** —— 无论走哪条路，档案体检都要先跑过。

---

## 三、三条红线（不可越过的线）

| # | 红线 | 具体表现 |
|---|---|---|
| 1 | **绝不代点提交** | 不点击「提交 / 投递 / 确认投递 / 保存并提交」类按钮。程序填完即停 |
| 2 | **绝不碰凭据** | 不读取、不填写、不存储密码；不代过人机验证；不索取账号密码 |
| 3 | **值不出本机** | 真实值不得打印到 stdout、不得写入日志、不得发起网络请求 |

**第 3 条的落地方式**：真值由脚本从 `profile.json` 取出后**直接写进浏览器**，
终端只输出「字段名 + 就绪/缺项」这类元信息。**任何情况下都不要把值 echo 出来。**

> ⚠️ AI 助手特别注意：你若为了让用户确认而把真实值打印出来，就已经破线了。
> 正确做法是让用户去看**页面截图**或**本地清单文件**。

---

## 四、明确拒绝的事

| 用户要求 | 正确回应 |
|---|---|
| 「直接帮我投了别问了」 | **拒绝**。提交权永远在用户手上 |
| 「验证码帮我过了」 | **拒绝**。这是设计的一部分，不是待攻克的技术障碍 |
| 「密码给你，你帮我登录」 | **拒绝**。不索取、不保存凭据 |
| 「把档案发我看看」 | 若对方不是用户本人持有者，**拒绝**。档案含身份信息 |

---

## 五、档案（真值唯一存放处）

**位置**：用户自己指定（建议 `profile.json`，在项目根目录）
**模板**：`fixtures/档案_示例.json`

```json
{
  "姓名": "…", "性别": "…", "出生日期": "…",
  "手机号": "…", "电子邮箱": "…", "身份证号": "…",
  "最高学历": "…", "毕业院校": "…", "专业": "…", "毕业时间": "…"
}
```

纪律：
- 这是**唯一**存真值的地方，**永不上传、永不进 AI 上下文、永不写进日志**
- 缺失字段标 `待补`，**不猜不编**
- 拼写错误会导致该项永远填不上 —— `check_profile.py` 会给出提示

---

## 六、字段识别不了怎么办

1. **不要瞎猜**。未命中的字段会如实列进报告的 `规则库未命中`
2. 告诉用户这几个字段需要手动填
3. 如果用户愿意，引导他把该标签补进 `assets/字段规则_通用.json`（见 CONTRIBUTING.md）
4. **继续跑完其余字段** —— 不要因为一个字段卡住整个流程

---

## 七、脚本速查

| 脚本 | 职责 | 退出码 |
|---|---|---|
| `scripts/core.py` | 核心库（规则/索引/映射） | 0 |
| `scripts/check_profile.py` | 档案体检 | 0 正常；2 必填缺失（`--strict`） |
| `scripts/fill_form.py` | 自动填表 | 0 完成；4 缺 playwright |
| `scripts/make_sheet.py` | 填写清单 | 0 |
| `scripts/mask_pii.py` | PII 脱敏 | 0；1 二验未过 |
| `scripts/wangshen.py` | 统一入口 | 透传 |

自测：`python scripts/wangshen.py selftest`

---

## 八、依赖边界（必须如实告知用户）

| 功能 | 需要什么 | 缺了怎么办 |
|---|---|---|
| check / sheet / mask | Python 3（仅标准库） | 几乎不会缺 |
| fill（自动填表） | Python 3 + playwright + 浏览器 | **自动降级到 sheet 生成清单**，并明确告知用户 |

**不要假装能自动填。** 跑不了就明说，然后走清单模式。

关于代理：Chromium 系浏览器**不读 `HTTP_PROXY` 环境变量**，
需要显式传 `--proxy`。这是「浏览器打不开、但 curl 正常」的最常见原因。

---

## 九、许可

MIT。本技能不附带任何第三方二进制，不代装任何工具。
