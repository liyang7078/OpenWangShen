# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [0.1.0] - 2026-09-20

首个可用版本。

### 新增

- **核心库 `core.py`**：字段规则加载、标签归一化索引、字段映射
  （精确匹配 + 包含匹配，短标签防误命中）
- **档案体检 `check_profile.py`**：分组覆盖统计、必填项缺失提示、
  档案拼写错误检测；**只报告字段名，不显示值**
- **自动填表 `fill_form.py`**：playwright 驱动，打开岗位页 →
  扫描表单 → 映射 → 写入 → 截图；支持 `--dry-run` 预演
  - 人工门自动识别：密码框 / 验证码 / 附件上传三类一律不碰
  - 日期格式自动归一（`2026-07` → `2026-07-01`）并标注「请核对」
  - 单选/复选按选项文本匹配，下拉框支持文本与值双路匹配
  - 缺 playwright 时自动降级提示，不报错崩溃
- **填写清单 `make_sheet.py`**：保底模式。生成人类可读的对照清单，
  **零浏览器依赖**；附件类字段单独成节；默认不覆盖同名文件
- **脱敏通道 `mask_pii.py`**：白名单策略 + 二验残留扫描 + 一键还原
- **统一入口 `wangshen.py`**：`check` / `fill` / `sheet` / `mask` /
  `selftest` / `guide` 六个子命令
- **字段知识库**：56 个字段 / 237 条标签，覆盖基本信息、联系方式、
  教育背景、能力证书、求职意向、经历与自述、附件七大分组
- **测试**：34 项单元测试（含反证用例）+ 4 个脚本的内置自测
- **测试夹具**：示例档案、模拟网申页（含各类控件与人工门字段）

### 三条设计红线（自首版起不可让步）

1. 绝不代点提交
2. 绝不填密码、绝不碰验证码
3. 身份信息不出本机（值不打印、不落日志、不发网络请求）

### 修复

- **radio / checkbox 组的字段名提取错误**（首版内修复，未发布）
  - 症状：单选组的字段名被取成**选项文本**（"男" / "是"），而不是组名
    （"性别" / "是否服从调剂"）
  - 此前之所以「看起来能填」，是因为「是」恰好是「是否服从调剂」的子串 ——
    **歪打正着**，一旦加严匹配规则就立刻暴露
  - 修法：对 radio / checkbox 改为向上查找**组容器中不含输入控件的 label** ——
    那才是真正的字段名；找不到再退化到通用提取
  - 效果：模拟网申页「规则库未命中」从 2 降为 0，仍保持 22/22 填入成功

- **档案体检把附件类字段误算为必填缺失**
  - 附件（简历 / 成绩单 / 证件照）是**文件**，不存放在档案 JSON 里
  - 修法：体检排除 `附件_` 前缀字段，单独成节提示

### 已验证

- 模拟网申页端到端：26 控件 → 可填 22 / 人工门 4 / 未命中 0，**22/22 填入成功**
- 真实站点：中国移动招聘站（岗位列表 → 岗位详情 → 投递入口）可打开、可扫描
- Moka 登录页密码框被正确识别为人工门
