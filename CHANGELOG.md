# 更新日志

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [0.1.1] - 2026-09-21

发布前的缺陷修复轮次。以下 6 项**全部由独立彩排实测发现** ——
把仓库原样克隆一份、完全照 README 走一遍陌生人的路，不碰源仓库。
其中 1 项高危（退出码失真）、1 项对外文档错误、4 项输出规范问题。

### 修复

- **填表收尾卡死 → 退出码失真**（高危）
  - 症状：表格已全部填对，但收尾的 `browser.close()` 卡住约 100 秒，
    进程被外层超时杀掉，**退出码变成 124 而不是 0** ⇒ 自动化无法据退出码判成败
  - 根因：原有的 `try/except` 兜不住「卡死」—— 卡住不是异常
  - 修法：收尾前挂**守护线程保险丝**。正常关闭是毫秒级、不会触发；
    超过 15 秒才结束进程，并以「填写结果」决定退出码，同时打印明示提示
  - 效果：退出码 **124 → 0**，耗时 **200 秒（超时被杀）→ 21 秒**

- **收尾保险丝的连带问题：产物被跳过**
  - 症状：保险丝生效时 `--out` 指定的报告**没有落盘** ——
    保住了退出码，却丢了产物，等于修复只做了一半
  - 根因：产物落盘写在 `with` 块之后，保险丝的 `os._exit()` 一跳过就没了
  - 修法：把产物落盘整体移到「武装保险丝之前」

- **`selftest` 有副作用：会弄脏工作区**
  - 症状：跑一次自测就改写仓库里**已提交**的示例文件（时间戳被刷新），
    `git status` 立刻不再干净 —— 谁跑谁脏
  - 修法：自测产物改落系统临时目录，绝不回写仓库

- **`guide` 输出被源码缩进污染**
  - 症状：4 条示例命令被挤成一行、多出 12 个空格缩进 ——
    而这恰恰是新用户打开项目**最先看到**的内容
  - 根因：普通三引号字符串里的 `\` 续行会吃掉换行
  - 修法：改写为 `\\`，换行得以保留、续行反斜杠正常显示

- **输出行尾符统一为 LF**（4 处写文件）
  - 症状：Windows 上产物写成 CRLF，与仓库 `.gitattributes` 的 `eol=lf` 冲突；
    脱敏稿与原文「逐行全变」，比对时必须额外忽略行尾符
  - 修法：写文件统一加 `newline=""`，关掉平台换行转换
  - 涉及：脱敏稿、填表报告 JSON、填写清单、自测产物

- **文档字段数写错**：`237 条标签` → `240 条标签`（5 处）
  - 涉及 `README.md`、`README.en.md`、`docs/快速开始.md`、`CHANGELOG.md`
  - 规则库实测共 **56 个字段 / 240 条标签**，文档里写小了一个数

### 已验证

全部修复在**原样克隆的独立副本**上复跑一遍（不碰源仓库）：

- `selftest` 4 项全通过；`unittest` **34 项 OK**；`py_compile` 全通过
- 端到端填表：26 控件 → 可填 22 / 人工门 4 / 未命中 0，**22/22 填入成功**，`exit=0`
  - 附件、验证码、密码原样留空；提交按钮未被触碰
- **修复前后的填表截图逐字节相同**（md5 一致）⇒ 无行为回归
- 脱敏 → 还原：**逐字节完全相同**（不再需要忽略行尾符）
- 跑完 `selftest` 后 `git status` 仍恰好是改动集本身 ⇒ 自测确认无副作用

---

## [0.1.0] - 2026-09-20

首个可用版本。

### 新增

- **核心库 `core.py`**：字段规则加载、标签归一化索引、字段映射
  （精确匹配 + 包含匹配，短标签防误命中）
- **档案体检 `check_profile.py`**：分组覆盖统计、必填项缺失提示、
  档案拼写错误检测；**只报告字段名，不显示值**
- **自动填表 `fill_form.py`**：playwright 驱动，打开岗位页 →
  扫描表单 → 映射 → 写入 → 截图；支持 `--dry-run` 预演
  - 人工门自动识别：密码框 / 验证码 / 附件上传三类一律不碰
  - 日期格式自动归一（`2026-07` → `2026-07-01`）并标注「请核对」
  - 单选/复选按选项文本匹配，下拉框支持文本与值双路匹配
  - 缺 playwright 时自动降级提示，不报错崩溃
- **填写清单 `make_sheet.py`**：保底模式。生成人类可读的对照清单，
  **零浏览器依赖**；附件类字段单独成节；默认不覆盖同名文件
- **脱敏通道 `mask_pii.py`**：白名单策略 + 二验残留扫描 + 一键还原
- **统一入口 `wangshen.py`**：`check` / `fill` / `sheet` / `mask` /
  `selftest` / `guide` 六个子命令
- **字段知识库**：56 个字段 / 240 条标签，覆盖基本信息、联系方式、
  教育背景、能力证书、求职意向、经历与自述、附件七大分组
- **测试**：34 项单元测试（含反证用例）+ 4 个脚本的内置自测
- **测试夹具**：示例档案、模拟网申页（含各类控件与人工门字段）

### 三条设计红线（自首版起不可让步）

1. 绝不代点提交
2. 绝不填密码、绝不碰验证码
3. 身份信息不出本机（值不打印、不落日志、不发网络请求）

### 修复

- **radio / checkbox 组的字段名提取错误**（首版内修复，未发布）
  - 症状：单选组的字段名被取成**选项文本**（"男" / "是"），而不是组名
    （"性别" / "是否服从调剂"）
  - 此前之所以「看起来能填」，是因为「是」恰好是「是否服从调剂」的子串 ——
    **歪打正着**，一旦加严匹配规则就立刻暴露
  - 修法：对 radio / checkbox 改为向上查找**组容器中不含输入控件的 label** ——
    那才是真正的字段名；找不到再退化到通用提取
  - 效果：模拟网申页「规则库未命中」从 2 降为 0，仍保持 22/22 填入成功

- **档案体检把附件类字段误算为必填缺失**
  - 附件（简历 / 成绩单 / 证件照）是**文件**，不存放在档案 JSON 里
  - 修法：体检排除 `附件_` 前缀字段，单独成节提示

### 已验证

- 模拟网申页端到端：26 控件 → 可填 22 / 人工门 4 / 未命中 0，**22/22 填入成功**
- 真实站点：中国移动招聘站（岗位列表 → 岗位详情 → 投递入口）可打开、可扫描
- Moka 登录页密码框被正确识别为人工门
